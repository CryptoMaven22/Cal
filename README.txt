CAL SITE UPDATE

1. Replace the existing index.html in the root of the Cal GitHub repository with this index.html.
2. Commit and push the change to the production branch, usually main.
3. Vercel will redeploy automatically if the repository is connected.

The Cal logo is embedded directly in index.html, so no separate image file is required.
